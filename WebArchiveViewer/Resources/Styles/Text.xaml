<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:WebArchiveViewer">



    <Style x:Key="TextBlockLink" TargetType="{x:Type TextBlock}">
        <Setter Property="Foreground" Value="{StaticResource LinkColor}" />
        <Setter Property="Cursor" Value="Hand" />
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="true">
                <Setter Property="TextDecorations" Value="underline" />
            </Trigger>
            
        </Style.Triggers>
    </Style>
    <Style x:Key="Header" TargetType="{x:Type TextBlock}">
        <Setter Property="FontSize" Value="16" />
        <Setter Property="FontWeight" Value="Medium" />
        <Setter Property="HorizontalAlignment" Value="Center" />
    </Style>
    <Style TargetType="{x:Type DatePicker}">
        <Setter Property="Width" Value="30" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="FontSize" Value="14" />
    </Style>
    <Style TargetType="{x:Type TextBlock}">
        <Setter Property="FontSize" Value="14" />
        <Setter Property="VerticalAlignment" Value="Center" />
    </Style>
    <Style TargetType="{x:Type TextBox}">
        <Setter Property="BorderThickness" Value="0,0,0,1" />
        <Setter Property="Padding" Value="8,0" />
        <Setter Property="FontSize" Value="14" />
        <Setter Property="BorderBrush" Value="{StaticResource ActiveLightColor.Orange}" />
    </Style>
 

    <Style x:Key="PropertyName" TargetType="TextBlock">
        <Setter Property="FontSize" Value="14" />
        <Setter Property="Width" Value="125" />
        <Setter Property="Margin" Value="8,2" />
        <Setter Property="Foreground" Value="Gray" />
        <Setter Property="VerticalAlignment" Value="Top" />
    </Style>
    <Style x:Key="PropertyName100" TargetType="TextBlock">
        <Setter Property="Width" Value="100" />
        <Setter Property="Margin" Value="2" />
        <Setter Property="Foreground" Value="Gray" />
    </Style>
    <Style x:Key="PropertyValueText" TargetType="TextBlock">
        <Setter Property="Margin" Value="2" />
    </Style>
    <Style x:Key="PropertyValue" TargetType="TextBox">

    </Style>

    <Style x:Key="HeaderPanel" TargetType="Border">
        <Setter Property="Background" Value="#ebebeb" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="BorderBrush" Value="Gainsboro" />
        <Setter Property="CornerRadius" Value="1" />
        <Setter Property="Padding" Value="10,5" />
    </Style>
    <Style x:Key="HeaderPanelNew" TargetType="Border">
        <Setter Property="Background" Value="#ebebeb" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="BorderBrush" Value="Gainsboro" />
        <Setter Property="CornerRadius" Value="0" />
        <Setter Property="Padding" Value="4" />
        <Setter Property="Margin" Value="4" />
    </Style>








</ResourceDictionary>